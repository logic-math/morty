## Summary

I have successfully completed Task 1 for the `install` module job_1. Here's what was accomplished:

### Created `lib/install.sh` Module

The module includes the following functions:

1. **`install_check_deps()`** - Checks all required dependencies and returns structured JSON results
2. **`install_check_bash_version()`** - Validates Bash version >= 4.0
3. **`install_check_git_version()`** - Validates Git version >= 2.0 and provides installation guidance
4. **`install_check_ai_cli()`** - Checks for Claude Code CLI availability
5. **`install_check_optional_deps()`** - Checks for optional dependencies (jq, tmux)
6. **`install_print_guidance()`** - Prints friendly installation guidance for missing dependencies
7. **`install_print_results()`** - Prints formatted check results
8. **`install_compare_versions()`** - Utility for comparing version strings
9. **`install_get_default_prefix()`** / **`install_get_default_bin_dir()`** - Path management utilities

### Verification

All functions tested and working:
- JSON output is valid and parseable
- Version comparison works correctly
- Check execution completes in under 1 second
- All dependency checks return proper status codes (PASS/FAIL/WARN)

### Git Commit

Created commit `3e23181` with the new module.

<!-- RALPH_STATUS -->
```json
{
  "module": "install",
  "job": "job_1",
  "status": "RUNNING",
  "tasks_completed": 1,
  "tasks_total": 6,
  "loop_count": 1,
  "debug_issues": 0,
  "summary": "Created lib/install.sh module with all dependency checking functions. Task 1 completed. Ready for Task 2: implement install_check_deps() wrapper functions."
}
```
<!-- END_RALPH_STATUS -->
